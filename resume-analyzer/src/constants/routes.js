/**
 * constants/routes.js
 * Single source of truth for all client-side route paths.
 * Import ROUTES anywhere instead of writing '/results' as a raw string.
 */
export const ROUTES = {
  LOGIN:    '/login',
  SIGNUP:   '/signup',
  UPLOAD:   '/',
  RESULTS:  '/results',
  INSIGHTS: '/insights',
  JOBS:     '/jobs',
};
